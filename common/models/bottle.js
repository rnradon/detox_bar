'use strict';

module.exports = function(Bottle) {
	// var bottle = Bottle.validatesUniquenessOf('ingredient_name', {message: 'ingredient name is not unique'});

	// bottle.isValid(function (valid) {
	//   if (!valid) {
	//     bottle.errors // hash of errors {attr: [errmessage, errmessage, ...], attr: ...}
	//   }
	// });
};